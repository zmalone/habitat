steps:
  #
  # Shellcheck!
  #
  - label: "Shellcheck :bash:"
    command: 
      - ls -la
      - ./test/shellcheck.sh
    agents:
      queue: 'docker-privileged'
    plugins:
      docker#v2.1.0:
        image: "chefes/buildkite"